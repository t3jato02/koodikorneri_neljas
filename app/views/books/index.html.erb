<h1>Books</h1>

<%= link_to "Back", root_path %>

<ul>
  <% @books.each do |book| %>
    <li>
      <strong>
        <%= book.name %>
      </strong>
      <%= book.author %>

      <%= link_to "Show details", book_path(book) %>
    </li>
  <% end %>
</ul> 

